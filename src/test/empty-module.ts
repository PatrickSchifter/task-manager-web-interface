// Stub vazio para o pacote `server-only` durante os testes — ele lança um erro
// quando importado fora de um Server Component, o que quebraria os testes de
// módulos server-only (ex.: lib/auth/session.ts).
export {};
